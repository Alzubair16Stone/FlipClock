import type { ChangeColor } from "../types/index";
import { Menu } from "lucide-react";
const ChangeColors = () => {

    return (
        <div className="flex justify-end">
            <Menu className={`border-2 border-dashed border-deepNavy p-5]`} />
            <h1 className="text-deepNavy bg-deepNavyBg">hgasgfs</h1>
        </div>
    )
}

export default ChangeColors